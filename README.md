1. npm init -y //esto es para crear el servidor node
2. npm install --save-dev jest //instalar framework JEST para testeo
   3.npm install --save-dev jest supertest //instalacion del supertest
   payasosRepository.integration.test
